# Cartella immagini dentro il repository GitHub
case_root = "CASI_RM"

# Inserisci qui la tua stringa PostgreSQL completa.
# Esempio Neon / Postgres generico:
# database_url = "postgresql+psycopg2://USER:PASSWORD@HOST/DBNAME?sslmode=require"
database_url = "postgresql+psycopg2://USER:PASSWORD@HOST/DBNAME?sslmode=require"
